import { Page, Browser } from 'playwright'; // Adjust the import path if necessary

export const fixture = {
    //@ts-ignore
    page: undefined as Page,
    //@ts-ignore
    browser: undefined as Browser 
}